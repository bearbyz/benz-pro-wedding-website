import type { Metadata } from "next";
import InvitationSuite from "@/components/invitation-suite";

export const metadata: Metadata = { title: "จดหมายแห่งความรัก — เบนซ์ & โปร" };
export default function SuitePage() { return <InvitationSuite/>; }
